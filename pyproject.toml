[project]
name = "blog-site"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.2",
]

[dependency-groups]
dev = [
    "ruff>=0.11.13",
]
